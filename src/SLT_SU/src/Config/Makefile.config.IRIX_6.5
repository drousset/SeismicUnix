
OPTC = -g 
OPTC =  -64 -O3

OPTF = -g 
OPTF  = -64 -O3

CFLAGS = $(OPTC) -I$(SLTROOT)/cwp/include -I$(SLTROOT)/slt/include
FFLAGS = $(OPTF)

LPATH=  -64                              \
      -L$(SLTROOT)/cwp/src/cwp/lib        \
      -L$(SLTROOT)/cwp/src/par/lib        \
      -L$(SLTROOT)/cwp/src/su/lib         \
      -L$(SLTROOT)/cwp/src/xplot/lib      \
      -L$(SLTROOT)/slt/src/fxymig/lib     \
      -L$(SLTROOT)/slt/src/grid/lib       \
      -L$(SLTROOT)/slt/src/ktmig/lib      \
      -L$(SLTROOT)/slt/src/kzmig/lib      \
      -L$(SLTROOT)/slt/src/rayt3d/lib     \
      -L$(SLTROOT)/slt/src/su/lib         \
      -L$(SLTROOT)/slt/src/umisc/lib      \
      -L$(SLTROOT)/slt/src/velo/lib       \
      -L$(SLTROOT)/slt/src/wgc/lib        \

LDLIBS = $(LPATH)  \
        -lwgc      \
        -lgrid     \
        -lvelo     \
        -lumisc    \
        -lusu      \
        -lxplot    \
        -lsu       \
        -lpar      \
        -lcwp      \
        -lXt       \
        -lX11      \
        -lm        \
        -lftn      \
        -lc        \

