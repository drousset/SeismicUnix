The contents are the following compressed tarfiles:

cwp.tar.Z       cwp,par
plot.tar.Z      x,ps graphics
xcwp.tar.Z      xgraphics using toolkit
su.tar.Z        SU
extrap.tar.Z    Dave Hale's extrapolation code
wx.tar.Z        Zhiming Li's wximplicit modeling and migration

Please note that the codes should be installed on your
system in the order listed above.

The extraction procedure is as follows:

% uncompress *.tar.Z

% tar -xvf filename.tar

