#!/bin/sh -ve
make junk
#chart <junk
#suchw < junk key2=sx key3=offset c=1 key1=gx | chart
#plot <junk
#sutpow tpow=2 <junk | plot

susort<junk|sunmo vel=1500|sustack|sutpow tpow=2|sutee jstack.brute|supef tol=48>jpsd

#echo "Hit return to see plots"
#read answer
#echo Plotting jstack.brute ...
#sleep 2
#plot <jstack.brute

pause see junk
plot <junk

pause see jstack.brute
plot <jstack.brute

pause see jpsd
plot <jpsd

<junk \
	susort offset sx | \
	sutpow tpow=2 | \
	supef tol=48 | \
	suchw key1=gx key2=sx key3=offset c=1 | \
	suchw key2=sx key3=gx c=1 d=2 | \
	sutee jdata | \
	sunmo vel=1500 | \
	sutee jdata.nmo | \
	sudmo \
>jdata.dmo

susort cdp <jdata     | sustack >jstack
susort cdp <jdata.nmo | sustack >jstack.nmo
susort cdp <jdata.dmo | sustack >jstack.dmo

echo "Plots?"
read answer
echo $answer
#if($answer != "y" ) then exit; fi

plot <jstack
plot <jstack.nmo
plot <jstack.dmo

exit
